{
  "$schema": "https://schema.tauri.app/config/2",
  "build": {
    "beforeBuildCommand": "bun run bundle:plugins && bun run build && bun run build:cookie-helper"
  },
  "bundle": {
    "externalBin": [
      "binaries/openusage-cookie-helper"
    ]
  }
}
