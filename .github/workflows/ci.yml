name: CI

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  check:
    name: Lint, Type-check, Build, Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.6"

      - run: bun install

      - name: Test Cookie Helper
        run: bun run test:cookie-helper

      - name: Type-check and build
        run: bun run build

      - name: Run tests
        run: bun run test

      - name: Test updater signature script
        run: node --test scripts/verify-updater-signature.test.mjs

  macos-cookie-helper:
    name: Cookie Helper (${{ matrix.target }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-15
            target: aarch64-apple-darwin
          - platform: macos-15-intel
            target: x86_64-apple-darwin
    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.6"

      - run: bun install --frozen-lockfile

      - name: Test Cookie Helper
        run: bun run test:cookie-helper

      - name: Build Cookie Helper
        run: bun run build:cookie-helper -- ${{ matrix.target }}

      - name: Verify Cookie Helper
        run: bun run verify:cookie-helper-build -- ${{ matrix.target }}

  windows:
    name: Windows Build And NSIS
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - uses: swatinem/rust-cache@v2
        with:
          workspaces: "./src-tauri -> target"

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.6"

      - run: bun install

      - name: Bundle plugins
        run: bun run bundle:plugins

      - name: Run tests
        run: bun run test --run

      - name: Type-check and build frontend
        run: bun run build

      - name: Test Rust library
        run: cargo test --manifest-path src-tauri/Cargo.toml --lib

      - name: Check Windows binaries
        run: cargo check --manifest-path src-tauri/Cargo.toml --target x86_64-pc-windows-msvc --bins

      - name: Build unsigned NSIS installer
        run: >-
          bun tauri build
          --target x86_64-pc-windows-msvc
          --bundles nsis
          --config '{"bundle":{"createUpdaterArtifacts":false}}'
          --ci
